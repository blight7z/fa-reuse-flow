# Sample import workbooks

These workbooks contain synthetic portfolio data only.

- `buyback-items-valid.xlsx` passes the import rules and is used for the happy-path demo.
- `buyback-items-errors.xlsx` intentionally contains a missing model, zero quantity, invalid condition, and duplicate serial number.

Import the `Items` sheet. The `Instructions` sheet documents every required column.

