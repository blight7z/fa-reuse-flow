# Browser calls the same-origin Next.js proxy by default.
NEXT_PUBLIC_API_BASE_URL=/api/v1
# Server-side proxy destination; use http://api:8000 in Docker Compose.
INTERNAL_API_URL=http://localhost:8000
